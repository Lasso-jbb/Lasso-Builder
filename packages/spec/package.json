{
  "name": "@lasso/spec",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Den deklarative visnings-spec: kriterier, komponentkatalog og datamodeller. Delt af server og UI.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "zod": "^4.2.0"
  }
}
